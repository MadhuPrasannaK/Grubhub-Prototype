let uploader;_375‍.x([["handleUpload",()=>handleUpload]]);_375‍.w("../config/cloudinary",[["uploader",["uploader"],function(v){uploader=v}]]);



const handleUpload = async (file) => {
    let result = await uploader.upload(file, {
        transformation: [{
            width: 175,
            height: 125,
            crop: "scale"
        }]
    })
    const image = result.url;
    return ({
        image
    })
}


