let Restaurants,Dishes,_;_375‍.x([["handle_request",()=>handle_request]]);_375‍.w("../../models/restaurant",[["default",["Restaurants"],function(v){Restaurants=v}]]);_375‍.w("../../models/dish",[["default",["Dishes"],function(v){Dishes=v}]]);_375‍.w("lodash",[["default",["_"],function(v){_=v}]]);



const handle_request = async (restaurant_id, callback) => {
    let restaurant = await Restaurants.findOne({
        _id: restaurant_id
    })
    if (!restaurant) {
        callback({
            message: "No restaurant found in DB!"
        }, null)
    }
    let allDishes = await Dishes.find({
        _id: {
            $in: restaurant.dishes
        }
    })
    if (!allDishes || !allDishes.length) {
        callback(null, {})
    }
    const groupedDishes = _.chain(allDishes).map().groupBy('section').map((value, key) => ({
        section: key,
        _id: value[0]._id,
        dishes: value
    })).flatten().sortBy(each => each.section.toLowerCase()).value();
    _375‍.g.console.log(groupedDishes)
    if (!callback) return groupedDishes
    callback(null,
        groupedDishes
    )
}



