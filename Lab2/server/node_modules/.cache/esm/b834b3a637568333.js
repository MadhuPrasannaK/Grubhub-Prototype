let Restaurants;_375‍.x([["handle_request",()=>handle_request]]);_375‍.w("../../models/restaurant",[["default",["Restaurants"],function(v){Restaurants=v}]]);

const handle_request = async (restaurant_id, callback) => {
    let restaurant = await Restaurants.findOne({
        _id: restaurant_id
    })
    if (!restaurant) {
        callback({
            message: "No restaurant in DB!"
        }, null)
    }
    if (!callback) return restaurant

    callback(null,
        restaurant
    )
}



