let Users,jwt,jwtSecret;_375‍.x([["handle_request",()=>handle_request]]);_375‍.w("../../models/user",[["default",["Users"],function(v){Users=v}]]);_375‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);_375‍.w("../../config/jwtConfig",[["default",["jwtSecret"],function(v){jwtSecret=v}]]);




const handle_request = async (userCredentials, callback) => {
    let user = await Users.findOne({
        email: userCredentials.email
    })
    if (!user) {
        callback({
            message: "User not registered!"
        }, null)
    }
    const token = jwt.sign({
        _id: user._id
    }, jwtSecret.secret)
    if (user.account_type === 'Vendor') {
        callback(null, {
            _id: user._id,
            first_name: user.first_name,
            last_name: user.last_name,
            email: user.email,
            account_type: user.account_type,
            phone: user.phone,
            address: user.address,
            image: user.image,
            token: token,
            restaurant_id: user.restaurant_id
        })
    } else {
        callback(null, {
            _id: user._id,
            first_name: user.first_name,
            last_name: user.last_name,
            email: user.email,
            account_type: user.account_type,
            phone: user.phone,
            address: user.address,
            image: user.image,
            token: token,
        })
    }
}



